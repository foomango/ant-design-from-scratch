import React from 'react'

import Button from '.'
import './style'

export default {
  title: 'Button',
  component: Button,
}

export const Basic = () => (
  <div>
    <Button>Default</Button>
  </div>
)
